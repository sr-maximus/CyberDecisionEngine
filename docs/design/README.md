# Design evidence policy

Interface screenshots are generated locally with synthetic data. They are not
stored in the public repository because they can expose organization names,
domains, account labels, run identifiers, dates, or measured results.

Before sharing a capture, confirm that all visible values use reserved example
names and domains and that no local account or operational evidence appears.
