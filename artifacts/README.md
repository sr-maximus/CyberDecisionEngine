# Local validation artifacts

Generated reports, screenshots, runtime identifiers, measured results, and
organization-specific evidence must remain local and must not be committed.

Use only synthetic RFC-reserved examples when documentation needs to show the
shape of an artifact. The `artifacts/` directory is ignored except for this
policy file.
